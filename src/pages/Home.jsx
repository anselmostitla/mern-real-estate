import React from 'react'

export default function Home() {
  return (
    <div>
      "".join(python_arr), txt[:], index, find, upper, lower, title, capitalize
      replace(old, new)
      isdigit, startswith, endswith
      min, max, sum, len, type, round, random
    </div>
  )
}
